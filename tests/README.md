# Test Suite

This directory contains the active, authoritative test suite for the
student_success package.

Legacy tests from earlier development have been preserved in
`tests_legacy/`. Those tests are not maintained and are not executed as
part of normal development. Useful tests from `tests_legacy` should be
updated and migrated here over time.