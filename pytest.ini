# pytest.ini
# This configuration ensures that the project root (.) is added to PYTHONPATH,
# allowing tests to import modules like `student_success.*` without import errors.
# Requires the `pytest-pythonpath` plugin to work correctly.
[pytest]
python_paths = .